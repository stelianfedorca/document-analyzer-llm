/*
    This need to be inserted as props to the ThemeProvider from styled-components
    !! Currently not used as I am using CSS variables !!
*/
export const theme = {
  colors: {
    primary: "blue",
  },
};
